# Graphite Theme

This is a declarative NiuPanel theme plugin. It contains no executable entry,
JavaScript, arbitrary CSS, database migration, or direct panel data access.

Package it with:

```bash
node scripts/package-plugin.mjs examples/plugins/theme-graphite --out dist/plugins
```
